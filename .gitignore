# Nothing to ignore
# But we'll pretend anyway

node_modules/
*.log
.DS_Store
dist/
build/
coverage/
.idea/
.vscode/

# Actually, ignore nothing (except the usual suspects)
# This way all our Nothing content is visible

# But we still ignore these normal things:
.claude/